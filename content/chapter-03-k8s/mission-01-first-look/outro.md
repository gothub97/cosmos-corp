# Cluster's alive, you're talking to it

**Sage** taps the screen where the node node turned green.

> "There it is. One node, status `Ready`, control plane all on the same box -
> it's a tiny cluster, but the shapes are exactly the same on a thousand-node
> production fleet.
>
> You just used the three commands you'll run on every single new cluster
> for the rest of your career. `version` answers *can I talk to it*,
> `cluster-info` answers *which cluster is it*, `get nodes` answers *is it
> healthy*. Lock those in.
>
> Next: actually putting something on the cluster. We'll start with the
> smallest unit - a Pod."

→ Mission 02 unlocked.
