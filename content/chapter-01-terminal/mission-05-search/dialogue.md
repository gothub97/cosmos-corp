# Searching the codebase

**Sage** drops a link to the `cosmos-svc` repo.

> "Real codebases are full of TODOs and half-finished docs. The two
> commands you'll reach for daily are:
>
> - `find` - *which files match this pattern?*
> - `grep` - *which lines contain this text?*
>
> And `wc` for *how many lines is that?* - surprisingly useful when you're
> sizing up a problem.
>
> I checked out a tiny version of `cosmos-svc` at `~/repo`. Let's pull
> together a quick TODO inventory for the team standup."
