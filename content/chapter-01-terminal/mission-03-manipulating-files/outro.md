# Tidy.

**Sage** glances at your `tree ~/scratch` and grins.

> "That's basically how every dev sets up their workspace.
> `mkdir` → `touch` → `cp` → `rm` is half of file management; the other
> half is **chaining commands together** - and that's what we'll do next."

→ Mission 04 unlocked.
