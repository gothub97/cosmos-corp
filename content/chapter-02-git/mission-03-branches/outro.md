# Branches feel weird at first

**Sage** is grinning.

> "If you noticed the file you committed disappear when you switched back
> to `main` - that's the magic. Your work isn't lost; it's just on the
> *other* branch. Switch back to `feature/intake-form` and it'll be there.
>
> Tomorrow we glue branches back together with `git merge`."

→ Mission 04 unlocked.
