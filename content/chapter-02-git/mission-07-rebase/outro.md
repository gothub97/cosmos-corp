# History sculpted

**Sage** tilts their head approvingly.

> "Pretty satisfying, right? Three forgettable commits became one
> readable one. Future you (and code reviewers) will thank you.
>
> Two rules to internalise:
>
> - **Rebase your own local commits, not other people's.** Once you've
>   pushed to a shared branch, rewriting history is rude - it forces
>   everyone else to deal with the rewrite.
> - **`git rebase --abort` is your friend.** If a rebase gets confusing
>   mid-flight, abort and start over. You can't break anything.
>
> Tomorrow: undoing things. `reset`, `revert`, and `restore` - three
> commands people constantly mix up."

→ Mission 08 unlocked.
