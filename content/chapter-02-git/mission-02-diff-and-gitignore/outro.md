# Cleaner already

**Sage** glances at your `git status`, satisfied.

> "Notice how `git status` is *quiet* now? That's the goal. Anything noisy
> should either be committed, deleted, or ignored. Nothing should sit in the
> 'untracked' list forever - that's where mistakes happen.
>
> Tomorrow: branches. The thing that lets you try something risky without
> blowing up the main line of work."

→ Mission 03 unlocked.
