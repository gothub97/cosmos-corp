# That's the whole loop

**Sage** raises an imaginary glass.

> "Congrats - you just did the thing every developer in the company does
> dozens of times a day. *Edit, status, add, commit, log.* That muscle
> memory is the foundation for everything else this week.
>
> Next up: how git shows you what's actually changed inside a file, and how
> to tell it to stop bothering you about files you don't care about (logs,
> build outputs, secrets). Time to meet `git diff` and `.gitignore`."

→ Mission 02 unlocked.
